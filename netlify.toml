# Root Netlify configuration - deploy frontend only
[build]
  base = "blink/frontend"
  command = "npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "20"
  # Ensure devDependencies (like vite) are installed
  NPM_CONFIG_PRODUCTION = "false"
  # You can still rely on process.env.NODE_ENV defaulting to 'production' during build

# SPA routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Security & caching headers
[[headers]]
  for = "/static/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/*.js"
  [headers.values]
    Content-Type = "application/javascript"

[[headers]]
  for = "/*.css"
  [headers.values]
    Content-Type = "text/css"

# (Optional) Add API proxy later if backend hosted elsewhere
